<% if planner.errors.any? %>
  <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded" role="alert">
    <p class="font-bold">There were some problems with your submission:</p>
    <ul class="list-disc list-inside">
      <% planner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: planner, local: true do |form| %>
  <div class="grid grid-cols-1 gap-6">
    <div>
      <label class="block text-lg font-medium text-gray-700 mb-2">Start Date</label>
      <%= form.date_field :start_date, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
    </div>
    <div class="grid grid-cols-2 gap-6">
      <div>
        <label class="block text-lg font-medium text-gray-700 mb-2">Technique</label>
        <%= form.text_field :technique, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
      </div>
      <div>
        <label class="block text-lg font-medium text-gray-700 mb-2">Technique BPM</label>
        <%= form.number_field :technique_bpm, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
      </div>

      <div>
        <label class="block text-lg font-medium text-gray-700 mb-2">Fretboard</label>
        <%= form.text_field :fretboard, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
      </div>
      <div>
        <label class="block text-lg font-medium text-gray-700 mb-2">Fretboard BPM</label>
        <%= form.number_field :fretboard_bpm, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
      </div>

      <div>
        <label class="block text-lg font-medium text-gray-700 mb-2">Repertoire</label>
        <%= form.text_field :repertoire, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
      </div>
      <div>
        <label class="block text-lg font-medium text-gray-700 mb-2">Repertoire BPM</label>
        <%= form.number_field :repertoire_bpm, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
      </div>
    </div>
    <div>
      <label class="block text-lg font-medium text-gray-700 mb-2">Note</label>
      <%= form.text_field :note, class: "w-full border-gray-300 rounded-lg shadow-sm focus:ring focus:ring-blue-200" %>
    </div>

    <div class="flex justify-between pt-4">
      <%= link_to "Cancel", user_home_path, class: "bg-gray-300 hover:bg-gray-400 text-gray-800 font-medium py-2 px-4 rounded-lg" %>
      <%= form.submit "Save", class: "bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-6 rounded-lg" %>
    </div>
  </div>
<% end %>
